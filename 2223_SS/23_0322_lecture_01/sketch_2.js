//add eyes and a mouth 
let x; //declare a variable for the x location
let y; //declare a variable for the y location

function setup() {
  createCanvas(500, 500);
}

function draw() {
  background(150);
  ellipse(250, 250, 200, 200);
}